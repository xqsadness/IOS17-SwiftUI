
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            //Jump to definition
            LayoutCaching()
        }
    }
}

#Preview {
    RootView{
        ContentView()
    }
}
